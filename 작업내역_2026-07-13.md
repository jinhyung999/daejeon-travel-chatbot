# 작업 내역 — 2026-07-13 (event/place 데이터 보강 + 버스 동선 탐색)

## 1. event 테이블 채움 — 대전 공연 데이터 추가

- `csv/daejeon_performances.json`(87건, KOPIS 계열 공연 데이터)을 event 테이블에 반영
- 신규 컬렉터: [collectors/daejeon_performance.py](collectors/daejeon_performance.py)
  - 기간 문자열(`2026.07.28 ~ 2026.07.28`)을 `YYYYMMDD`로 정규화
  - 종료일 미정("오픈런") 공연은 `end_date='99991231'` sentinel로 저장해 날짜 비교 쿼리와 호환되게 처리
  - 원본은 팀 컨벤션에 따라 `raw_daejeon_performance` 테이블에 원본 필드명 그대로 보존(`collected_at` 포함)
  - 공연장 이름으로 place 테이블에서 좌표 매칭 시도(확실한 경우만, 오매칭 방지)

## 2. event 날짜 형식 통일 + 종료 행사 정리

- 기존 event에 `20260401`(TourAPI)과 `2026-04-01`(미술관 크롤링) 형식이 혼재되어, `WHERE end_date >= '오늘'` 같은 문자열 비교 쿼리에서 미술관 전시 8건이 전부 걸러지는 버그를 발견·수정
- 신규 스크립트: [scripts/event_maintenance.py](scripts/event_maintenance.py) — 날짜 형식 통일, 종료 행사 삭제, 형식 검증을 한 번에 수행 (주기 재수집 후 재실행 권장)
- [collectors/museum_exhibition.py](collectors/museum_exhibition.py) 수정 — 앞으로 수집분도 `YYYYMMDD`로 저장하도록 변경(재발 방지). event_id 해시 입력은 기존 형식 유지해 재수집 시 중복 생성 방지
- 실행 결과: 과거 종료 행사 12건 삭제 (TourAPI 축제 11건 전부 2025년 종료분 + 미술관 전시 1건), 유효 event **95건**

## 3. event 주소/좌표 보강

- 신규 스크립트: [scripts/fill_event_address.py](scripts/fill_event_address.py)
  - 1순위: place 테이블에서 동일 이름(개칭 전 이름 별칭 포함) 매칭 → 주소+좌표 복사
  - 2순위: 공연장 20곳은 공식 홈페이지/KOPIS/지자체 사이트 웹 검색으로 주소 확인 후 매핑 테이블로 반영
  - 결과: address 결측 72건 → 3건(정보 미확인 소규모 공연장)으로 감소, 92/95건 주소 확보
  - 좌표는 24/95건만 확보 — 추후 지오코딩 API 보강 필요(과제로 남김)

## 4. place 테이블 중복 제거

- 신규 스크립트: [scripts/dedupe_place.py](scripts/dedupe_place.py) (dry-run 기본, `--apply`로 실제 반영)
- 판정 기준: 이름 정규화(공백 제거) 일치 + 좌표 50m 이내 또는 주소 완전 동일 (이름만 같고 멀리 있는 체인점/동명업소는 제외)
- 병합 규칙: 소스 우선순위(tourapi > daejeon_food > daejeon_shopping > mois_lodging > sbiz)가 높은 행을 대표로 남기고, 결측 필드는 제거되는 행에서 흡수 → 정보 손실 없이 병합
- 제거되는 행은 `place_removed` 테이블에 원본 그대로 백업(복구 가능)
- 결과: 36건 병합 (tourapi ↔ 대전맛집 중복 24건 병합 시 영업시간/휴무일/전화번호 복구 부수효과 있음), place 26,111 → 26,075건

## 5. 대전시립박물관 전시 크롤러 — 코드는 작성, 실행은 보류

- 신규 컬렉터: [collectors/daejeon_history_museum.py](collectors/daejeon_history_museum.py)
- `daejeon.go.kr` 도메인 전체가 현재 접속 불가(에이전트 환경·로컬 PC·일반 브라우저 모두 동일하게 타임아웃 확인 — 시청 사이트 자체 장애로 판단)라 **실제 수집은 아직 못 했고, DB 변경 없음**
- 안전장치 포함: 목록 파싱 결과가 0건이면 DB를 건드리지 않고 중단, `--dry` 옵션으로 미리보기 가능, 기존 event와 제목이 겹치면 자동 제외
- 사이트 정상화 후 로컬에서 `python collectors/daejeon_history_museum.py --dry` 실행 후 결과 확인 필요
- 참고: 대전예술의전당은 위 87건 공연 데이터에 이미 36건(38%) 포함되어 있어 별도 크롤링 불필요로 판단, 스킵
- 대안으로 문화포털(culture.go.kr) 통합 전시정보 오픈API를 조사했으나, 확인된 목록이 국립 기관 위주(국립현대미술관 등)라 대전시립박물관(지자체 소속) 커버리지는 불확실 — 크롤링이 현재로선 더 현실적인 방법으로 판단

## 6. place attraction 카테고리 정리 — 생활 오락시설 분리

- 신규 스크립트: [scripts/reclassify_attraction.py](scripts/reclassify_attraction.py) (dry-run 기본, `--apply`로 실제 반영)
- 배경: attraction 2,171건 중 2,073건이 소상공인 상가정보(sbiz) 기반이었는데, 실제 소분류를 까보니 노래방·PC방·독서실 등 관광과 무관한 생활 오락시설이 대부분이었음(중분류명 "유원지·오락"/"도서관·사적지"만 보고 매핑한 결과)
- 삭제 대신 새 카테고리 `entertainment`로 분리(2,062건: 노래방 1,050 / 독서실·스터디카페 336 / PC방 197 / 복권판매 173 / 기타오락장 150+74 / 전자게임장 68 / 바둑장기 12 / 비디오방 2)
- attraction에는 실제 TourAPI 관광지(98건) + 수상레저·낚시터(11건)만 남음 → **2,171건 → 109건**
- "기타 오락장"류는 소분류명이 모호해 100% 확신은 아님(주석에 명시) — 필요시 팀 재검토

## 7. TAGO 버스노선정보로 transport.routes 결측 해소 + 정류소 커버리지 확장

- 신규 컬렉터: [collectors/tago_bus_routes.py](collectors/tago_bus_routes.py) (dry-run 기본, `--apply`로 실제 반영)
- 사용자가 제공한 TAGO(국토교통부 국가대중교통정보센터) API 키로 대전 버스노선(cityCode=25) 128개 전수 조사
  - `BusRouteInfoInqireService/getRouteNoList` → 노선 목록
  - `BusRouteInfoInqireService/getRouteAcctoThrghSttnList` → 노선별 경유 정류소 순서(정류소명·좌표·순번·상하행)
- 기존 transport(대전시 자체 BIS API 수집, 1,425건)와 이름+좌표(30m) 매칭 → 매칭된 846건에 `routes` 채움
- **TAGO 전체 정류소가 3,061개로 기존 DB(1,425개)보다 훨씬 많다는 걸 발견** — "유성온천역 6번출구" 등 실존 정류소가 기존 DB에 아예 없었음. 매칭 안 된 2,099건은 삭제된 게 아니라 **원래 결측이었던 정류소**로 판단해 `tago_{nodeid}` ID로 신규 추가
- 결과: transport **1,425건 → 3,524건**, routes 채움률 0% → **2,945/3,524건(83.6%)**
- API 세션 제한("가용한 세션이 존재하지 않습니다 30/30") 대응을 위해 재시도·백오프 로직 추가
- `bus_route.collected_at` 컬럼 추가해 수집 시점(2026-07-13) 기록 — TAGO가 "실시간 갱신"이라고는 하나 지자체→중앙시스템 반영 지연 가능성이 있어, 최소한 기준 시점은 남겨둠(공공데이터포털 확인 결과 갱신주기 "실시간", 최종수정일 2025-06-16)
- 지하철정보 API(사용자 제공 두 번째 키)는 조사 결과 여전히 실패 — 같은 키로 등록된 API 2개(지하철정보/대전교통정보)가 전부 오류나는 반면 다른 키의 API 3개는 전부 정상이라, **키 미승인 상태로 추정**. data.go.kr 마이페이지에서 승인 상태 확인 필요

## 8. 노선별 정류소 순서 저장 + 버스 동선(직행/환승) 탐색 기능

- 신규 테이블: `bus_route`(노선 메타), `bus_route_stop`(노선×방향별 정류소 순서, PK: route_id+updowncd+node_order)
- 신규 스크립트: [scripts/build_route_stops.py](scripts/build_route_stops.py) — `tago_bus_routes.py`가 남긴 캐시(`data/raw/tago_route_stops_*.json`)를 재가공만 해서 채움(API 재호출 없음), 9,874건 저장(매칭 실패 0건)
- 신규 모듈: [app/transit.py](app/transit.py) — `find_bus_directions(출발위경도, 도착위경도)`
  - 근처 정류소 탐색(직선거리) → 같은 노선·방향에서 순서 비교로 직행 여부 판정 → 없으면 1회 환승 조합 탐색
  - 소요시간/배차간격은 범위 밖(원본 데이터에 없음), 환승은 1회까지만 지원
  - 실제 좌표로 검증 완료: 갤러리아 타임월드점→신세계 대전점(직행 911번 확인), 성심당→대전시립박물관(환승 경로 확인)
  - 아직 `app/main.py`(챗봇 응답 로직, 여전히 빈 파일)에는 연결 안 됨 — 독립 호출 가능한 함수로만 존재

## 다음 단계 후보

- 지하철정보 API 키 승인 상태 확인 후 재시도
- 대전시립박물관 사이트 복구 후 크롤러 실행 및 파싱 검증
- event 좌표 미확보 71건 지오코딩
- place 중복 제거 로직을 정기 유지보수 루틴에 포함(재수집 후 dry-run → apply)
- raw 테이블화 확장(현재 daejeon_food/daejeon_shopping/daejeon_performance만 raw 테이블 보유, TourAPI 등 나머지 소스는 파일(`data/raw/`)로만 존재)
- transport 매칭 안 된 579건(1,425건 중 routes 미확보) 원인 확인 — 폐지된 정류소이거나 명칭 차이 가능성
- `app/main.py` 추천 로직 구현 시 `app/transit.py`의 버스 동선 탐색 연결
- 버스 노선 데이터 정기 재수집 주기 정책 수립(도시 개발로 노선 변경 가능성 있음)
